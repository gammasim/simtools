Changed behavior of `--pack_for_grid_register` flag in `simtools-simulate-prod`:
- by default, sim_telarray histogram (`.hdata`) files are no longer included in the log-file tar package
- to include the histograms, add the flag `--pack_for_grid_register_hdata`
