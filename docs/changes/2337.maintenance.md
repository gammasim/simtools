Improve guidance for coding agents and generalize (allows to be used now with Copilot, Claude, Codex):
- add a generic `AGENTS.md` (replaces `.github/copilot-instructions.md`)
- move skills to `.agents/skills` and add a technical writer skill.
