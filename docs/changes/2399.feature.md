Retrieve and store transition energy between high- and low-energy interaction model directly from CORSIKA7 code and store it in `build_opts.yml`.
Takes into account differences between interaction models.
