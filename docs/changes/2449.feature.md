Add machine-readable comparison statistics and configurable plot formats to
`simtools-compare-productions`.
Compare selected array layouts independently and write each result to its own output directory.
