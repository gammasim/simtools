Add support for running `simtools-simulate-prod` with parameters sourced directly from a production job grid ECSV file.
Allows to use `--job_grid_file` / `--job_grid_row ` to select a specific row from a job grid file to run a simulation with the parameters defined in that row.
