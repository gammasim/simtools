Write log output to a log file to improved reproducibility (keep stream to stderr).
The file can either given through the command line (via `--log-file`) or is
generated automatically to be in `output-path / simtools-name-UTC-Time`.
