Introduce central immutable settings module for configuration args, db_config, and path definitions.
Update `simtel_path` usage through all modules. Introduce `corsika_path` and `corsika_executable`.
Ensure backwards compatibility to legacy simtools images.
