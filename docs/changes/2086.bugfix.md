Fixed unit-test DB mocking scoping so tests/unit_tests/db/* reliably use a real DatabaseHandler while other unit tests still mock DB access. This resolves CI failures where DatabaseHandler methods were inadvertently replaced by MagicMock, causing missing internal calls, incorrect return values, and expected exceptions not being raised.
