---
# Workflow Configuration
# SetParameterFromExternal::set_MST_mirror_2f_measurements_from_external
# Describes:
# - setting workflow (ACTIVITY:NAME)
# - meta data model for user input (DATAMODEL:USERINPUTSCHEMA)
# - meta data model for workflow output (DATAMODEL:TOPLEVELMODEL)
# - data format for workflow output data (PRODUCT:FORMAT)
# - axes definition for workflow output data (DATA_COLUMNS)
#
# This workflow description is provided by SimPipe and should
# not be changed by users
#

CTASIMPIPE:

  REFERENCE:
    VERSION: 0.1.0

  ACTIVITY:
    NAME: SetParameterFromExternal::set_MST_mirror_2f_measurements_from_external
    ID: null
    DESCRIPTION: "Set data columns for MST mirror 2f measurements"

  PRODUCT:
    FORMAT: ascii.ecsv
    DIRECTORY: null

  CONFIGURATION:
    logLevel: 'INFO'
    test: false

  DATA_COLUMNS:
    mirror_panel_id:
      description: |-
        mirror panel ID
      required_column: true
      unit: dimensionless
      type: string

    mirror_panel_radius:
      description: |-
        mirror panel radius
      required_column: true
      unit: cm
      type: float32

    psf:
      description: |-
        Spot size of mirror panel PSF at nominal distance
      required_column: true
      unit: cm
      type: float32

    psf_opt:
      description: |-
        Spot size of mirror panel PSF (best value, not at nominal distance)
      required_column: false
      unit: cm
      type: float32
...
